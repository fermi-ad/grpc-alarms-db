name: Rust Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-tests-and-coverage:
    uses: fermi-ad/.github/.github/workflows/rust-integration.yaml@f5b1570438803675367730e055d4a3e1983be9d1
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
    with:
      coverage-exclude: "|queries.rs" # These files just contain static constants, which are inlined at compile time. Because of this, the coverage reporter shows the source files as lacking coverage.
      pull-git-submodules: true
      references-internal-git-repo: true
